/* import { Injectable } from '@angular/core';

@Injectable({        // injectable is another decorator function similar to component decorator function. We need tis decorator only if service had dependencies in its constructor.
  providedIn: 'root'
})
class EmailService {

  constructor(logService: LogService{ }
}// Angular should be able to inject the the dependencies of this class into its constructor. 
*/